{{!-- 
<h1>Các đơn hàng của bạn</h1>
<ul action="/checkout/user-orders">
    {{#each orders}}
    <li>
        <p>Mã đơn hàng: {{this._id}}</p>
        <p>Người nhận: {{this.fullName}}</p>
        <p>Email: {{this.email}}</p>
        <p>Số điện thoại nhận hàng: {{this.phone}}</p>
        <p>Tỉnh: {{this.province}}</p>
        <p>Quận/ Huyện/ Thị xã: {{this.street}}</p>
        <p>Phường/ Thị Trấn: {{this.district}}</p>
        <p>Địa chỉ nhận hàng cụ thể: {{this.specificAddress}}</p>
    </li>
    {{/each}}
</ul> --}}
{{#if authenticated}}
            <div class="col-lg-12 table-responsive mb-5">
                <table class="table table-light table-borderless table-hover text-center mb-0">
                    <thead class="thead-dark">
                        <tr>
                            <th>Sản phẩm</th>
                            <th>Thành tiền</th>
                            <th>Người nhận</th>
                            <th>Số điện thoại</th>
                            <th>Địa chỉ nhận hàng</th>
                            <th>Đã giao hàng</th>
                        </tr>
                    </thead>
                    {{#each orders}}
                    <tbody class="align-middle">
                        <tr>
                            <td class="align-middle"><img src="/public/img/product-1.jpg" alt="" style="width: 50px;"> Product Name</td>
                            <td class="align-middle">150</td>                            
                            <td class="align-middle">{{this.fullName}}</td>
                            <td class="align-middle">{{this.phone}}</td>
                            <td class="align-middle">{{this.specificAddress}}</td>
                            <td class="align-middle" ><span class="green-check"></span></td>
                            <style>
                                .green-check::before {
                                    content: "\2714"; /* Mã Unicode của dấu tích xanh */
                                    color: #00FF00; /* Màu xanh */
                                    font-size: 24px; /* Kích thước font */
                                    margin-right: 10px; /* Khoảng cách với nội dung khác (tuỳ chọn) */
                                }
                            </style>
                        </tr>                       
                    </tbody>
                    {{/each}}
                </table>
            </div>
{{/if}}